import React from "react";
import "./style.css";

function Header(props) {
  return (
    <header>
      <h1>
        Click an image to get a point, but don't click the same picture twice or
        it is Game Over!
      </h1>
    </header>
  );
}

export default Header;
